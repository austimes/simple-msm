{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schema/autonomous_efficiency_tracks.schema.json",
  "title": "Autonomous efficiency tracks",
  "description": "Schema for rows in families/<family_id>/autonomous_efficiency_tracks.csv. Each row describes family-local exogenous efficiency drift that applies automatically to explicit base states in one milestone year.",
  "type": "object",
  "required": [
    "family_id",
    "track_id",
    "year",
    "track_label",
    "track_description",
    "applicable_state_ids",
    "affected_input_commodities",
    "input_multipliers",
    "delta_output_cost_per_unit",
    "cost_basis_year",
    "currency",
    "source_ids",
    "assumption_ids",
    "evidence_summary",
    "derivation_method",
    "confidence_rating",
    "double_counting_guardrail",
    "review_notes"
  ],
  "properties": {
    "family_id": {
      "type": "string",
      "description": "Canonical family identifier. Must match the owning folder and shared/families.csv."
    },
    "track_id": {
      "type": "string",
      "description": "Stable family-local identifier for the autonomous efficiency track."
    },
    "year": {
      "type": "integer",
      "enum": [2025, 2030, 2035, 2040, 2045, 2050],
      "description": "Milestone year represented by this row."
    },
    "track_label": {
      "type": "string",
      "description": "Human-readable name for the track."
    },
    "track_description": {
      "type": "string",
      "description": "Plain-language description of what the autonomous drift represents."
    },
    "applicable_state_ids": {
      "type": "string",
      "description": "JSON-encoded array of explicit state_id values that the track may modify."
    },
    "affected_input_commodities": {
      "type": "string",
      "description": "JSON-encoded array of commodity identifiers affected by the drift. Keep aligned one-for-one with input_multipliers."
    },
    "input_multipliers": {
      "type": "string",
      "description": "JSON-encoded array of numeric multipliers aligned one-for-one with affected_input_commodities."
    },
    "delta_output_cost_per_unit": {
      "type": "number",
      "description": "Additive change to output_cost_per_unit relative to the base state in the same family and year."
    },
    "cost_basis_year": {
      "type": "integer",
      "description": "Base year used to normalize delta_output_cost_per_unit."
    },
    "currency": {
      "type": "string",
      "description": "Currency or price basis label for delta_output_cost_per_unit, such as AUD_2024."
    },
    "source_ids": {
      "type": "string",
      "description": "JSON-encoded array of source IDs that should resolve in shared/source_ledger.csv."
    },
    "assumption_ids": {
      "type": "string",
      "description": "JSON-encoded array of assumption IDs that should resolve in shared/assumptions_ledger.csv."
    },
    "evidence_summary": {
      "type": "string",
      "description": "Short summary of the evidence base supporting this track row."
    },
    "derivation_method": {
      "type": "string",
      "description": "Brief note on how the row was derived, scaled, or interpolated."
    },
    "confidence_rating": {
      "type": "string",
      "enum": ["High", "Medium", "Low", "Exploratory"],
      "description": "Qualitative confidence class for the row."
    },
    "double_counting_guardrail": {
      "type": "string",
      "description": "Explicit note describing how double counting with pathway states or packages is avoided."
    },
    "review_notes": {
      "type": "string",
      "description": "Reviewer caveats, unresolved questions, or quality flags that should stay visible downstream."
    }
  }
}
