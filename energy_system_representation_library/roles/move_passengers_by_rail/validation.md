# Move passengers by rail

- 2025 demand anchor: `1 residual_activity`.
- Default method: `transport_rail_passenger__residual_incumbent`.
- Residual quantities are split from the prior transport-other calibration so passenger rail is no longer hidden inside an opaque aggregate residual.
- This is a residual stub, not an explicit rail technology model.
