# Move freight by marine

- 2025 demand anchor: `1 residual_activity`.
- Default method: `transport_marine_freight__residual_incumbent`.
- Residual quantities are split from the prior transport-other calibration so marine freight is no longer hidden inside an opaque aggregate residual.
- This is a residual stub, not an explicit shipping technology model.
